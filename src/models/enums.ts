export enum Ecosystem {
    docker = "docker",
    // npm = "npm",
    // pip = "pip",
    // maven = "maven",
    // gradle = "gradle",
  }
  
  export enum Schedule {
    daily = "daily",
    weekly = "weekly",
    monthly = "monthly",
  }
  
  export enum Version {
    v1 = 1,
    v2 = 2,
  }
  